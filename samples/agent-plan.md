# Implementation Plan

> [!NOTE]
> This plan was generated by an AI agent. Review before executing.

> [!WARNING]
> Don't edit generated files directly.

> [!TIP]
> Run the tests after each phase.

## Phase 1 — Core

- [x] Scaffold project
- [x] Build renderer
- [ ] Wire the editor
- [ ] Add export

## Phase 2 — Polish

- [ ] Verify all themes
- [ ] Sign the build

## Diff Preview

```diff
- remarkPlugins={[remarkGfm]}
+ remarkPlugins={[remarkGfm, remarkCallouts]}
```

## Multi-file Output

### src/lib/tasklist.ts

```ts
export function toggleTaskAtLine(content: string, line: number): string {
  return content;
}
```

### src/styles/callouts.css

```css
.callout { border-left: 3px solid var(--accent); }
```

## A command

```bash
bun run tauri dev
```
